# Implementation Plans

Generated by the improve skill on 2026-07-13 against commit `181a5e8`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

Package manager: **Bun**. Verification after plan 001: `bun run check`
(`typecheck` + `bun test`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Restore typecheck + minimal test/check scripts | P1 | M | — | DONE |
| 002 | Clear pending claim on reject (do not delete device) | P1 | S | — | DONE |
| 003 | Pending owner cannot delete host device | P1 | S | 002 (same handler; can combine) | DONE |
| 004 | Fail closed on missing/insecure JWT_SECRET | P1 | S | — | DONE |
| 005 | Require device auth on geo search/route/speed-limit | P1 | M | 001 recommended | DONE |
| 006 | Characterization tests for pairing/auth | P2 | M | 001; prefer 002+003 first | DONE (HTTP suite skips without Postgres) |
| 007 | Remove dead Google Maps path; fix README | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first** when possible — gives every later plan a green `bun run typecheck` / `bun test` gate.
- **002 and 003** touch the same `DELETE /devices/:id/claim` handler; implemented together.
- **006** HTTP pairing tests use `describe.skipIf(!hasDb)` when Postgres is unreachable; with Docker up they run the full matrix. Register responses may be `201`.
- **005** and **007** both touch README — merged in this execution pass.

## Findings considered and rejected

- **Spotify access token returned to device**: required by Web Playback SDK; harden later, not a bug plan.
- **Plaintext Spotify tokens in Postgres**: real at-rest concern; deferred for local single-user display.
- **CORS `origin: "*"`**: intentional for companion Bearer clients (not cookie CSRF).
- **Split monolithic `vehicle.ts`**: defer until after 001 + 006.
- **Encrypt localStorage device secrets**: defer unless a hardening pass is requested.
- **Pairing code entropy / rate limits (audit #6)**: not selected in this batch.
- **Nav route-snap perf (#8/#9)**: not selected in this batch; do after 001.
- **In-memory Spotify OAuth state (#11)**: not selected in this batch.
- **Pin `"latest"` deps (#12)**: not selected in this batch.

## Direction (not planned here)

- Companion app config loop (home/saved places / Spotify link UX).
- Explicit nav v1 product boundary doc.
- Real OBD/CAN vehicle signals (README already out of scope for now).
